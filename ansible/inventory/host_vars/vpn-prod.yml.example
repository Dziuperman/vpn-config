# Copy this file to ansible/inventory/host_vars/vpn-prod.yml and replace the values.
ansible_host: 198.51.100.10
ansible_user: root
# Preferred long-term access mode:
# ansible_user: xray-deploy
# ansible_ssh_private_key_file: ~/.ssh/vpn-prod_ed25519
# ansible_become: true

xray_server_address: vpn.example.com

# Optional hardening for non-root deployments.
# xray_manage_deploy_user: true
# xray_deploy_user_name: xray-deploy
# xray_deploy_user_authorized_keys:
#   - "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA..."

# Store secrets in ansible/inventory/host_vars/vpn-prod.vault.yml
# encrypted via Ansible Vault instead of this plain file.
